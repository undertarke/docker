version: '3.8'
services:
  cons-be-node:
    container_name: cons-be-node
    build: .  # build bằng Dockerfile
    ports:
      - 8080:8080
    
    networks:
      - node-network

  
networks:
  node-network:
    external: true # kết nối đến network đã tạo sẵn
      